import type { Metadata } from 'next';
import React from 'react';

export const metadata: Metadata = {
  title: 'New User',
  description: 'New user',
};
const NewUserPage = () => {
  return <div className="">New-user</div>;
};

export default NewUserPage;
